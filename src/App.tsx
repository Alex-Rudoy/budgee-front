import { BrowserRouter } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      {/* <Header />
      <Suspense fallback={<LoadingIcon />}>
        <Switch></Switch>
      </Suspense>
      <Footer /> */}
    </BrowserRouter>
  );
}

export default App;
